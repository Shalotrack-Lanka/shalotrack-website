The hero is now a still photograph, not video, so hero-highway.mp4 is no
longer referenced. If you want motion back, swap the <img> inside
.hero-media for a <video> tag using the same classes — the gradient and
layout will carry over unchanged.
